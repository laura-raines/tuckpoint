rules_version = '2';

// All reads and writes go through the Admin SDK in Next.js API routes /
// server components, which bypasses rules. No client SDK access exists,
// so deny everything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
